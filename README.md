# otus_basic_hm_five
Домашние задание № 5
Расчёт статистических характеристик последовательности.
Структура проекта:
Подсчёт статистики:
    абстрактный класс - istatics общий интерфейс
        max - подсчёт максимума
        min - подсчёт минимума
        mean - подсчёт арихметическое среднее
            TO DO:
            std - среднеквадратическое отклонение       - выполнил
            pct90 - 90 процентель                       - выполнил
            pct95 - 95 процентель
Получение данных - parsing (читает файл и возращает std::vector<double>)
Класс менеджер, объединяет получение данных, передачу данных классам статистики и вывода результата.

TO DO:
нужен метод для сортировки вектора для pct90 pct95  - выполнил
добавить проверку на время выполнения проекта       - выполнил
можно выполнить сборку статистики в текстовый файл (ms) а далее выполнить статистику.  

Работа над ошибками
1. По условию ДЗ:

Приложение должно в ходе своей работы считать всю
последовательность из стандартного ввода

А я вижу, что чтение происходит из файла (https://github.com/Evgen89Mt/otus_basic_hm_five/blob/main/src/main.cpp#L8). Надо бы поправить.

        - выполнил. (bool Manager::inputDoubles())

2. Это считается хорошим тоном пихать const везде, где ты не собираешься менять значение. Это, во-первых, контракт, а во-вторых, гарантия от бессонных ночей в дебаге. Так что смело пихай const везде. Начать можно отсюда (https://github.com/Evgen89Mt/otus_basic_hm_five/blob/main/include/min.hpp#L11) и далее по всему коду.

        - 

3. Кажется, что во всех методах eval() стоит бросать исключение, что m_flag == false. Иначе возникают непонятки, вроде не добавлял чисел, а что-то отдаёшь.

        - 

4. https://github.com/Evgen89Mt/otus_basic_hm_five/blob/main/src/percentile90.cpp#L14 -- это работает за O(N*logN), где N -- количество элементов в массиве. Можно найти персентиль за O(N). Подумаешь как? =) Кстати, это одна из стандартных задач на собеседовании.

        -

5. Аналогично здесь: https://github.com/Evgen89Mt/otus_basic_hm_five/blob/main/src/percentile95.cpp#L14

        - 

6. А кто m_count будет здесь (https://github.com/Evgen89Mt/otus_basic_hm_five/blob/main/src/std.cpp#L9) увеличивать?

        -